<!DOCTYPE html>
<html lang="es" data-bs-theme="dark">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Analizador Financiero{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>💰</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <style>
            .card-metric { border-left: 4px solid; }
            .card-metric.positive { border-left-color: #198754; }
            .card-metric.negative { border-left-color: #dc3545; }
            .card-metric.neutral { border-left-color: #0d6efd; }
            .priority-badge { font-size: 0.75rem; }
            .collection-item { position: relative; }
            .remove-item { position: absolute; top: 0.5rem; right: 0.5rem; }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom">
            <div class="container">
                <a class="navbar-brand" href="{{ path('dashboard') }}">💰 Analizador Financiero</a>
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="{{ path('dashboard') }}">Dashboard</a>
                    <a class="nav-link" href="{{ path('profile_edit') }}">Editar Perfil</a>
                </div>
            </div>
        </nav>

        <main class="container py-4">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

        <footer class="container text-center text-muted py-3 border-top">
            <small>Analizador Financiero — Hecho con Symfony {{ constant('Symfony\\Component\\HttpKernel\\Kernel::VERSION') }}</small>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
