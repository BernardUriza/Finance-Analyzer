{% extends 'base.html.twig' %}

{% block title %}Dashboard — Analizador Financiero{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">{{ profile.name }} <small class="text-muted">({{ profile.age }} años)</small></h1>
        <a href="{{ path('profile_edit') }}" class="btn btn-outline-primary">Editar Perfil</a>
    </div>

    {% include 'dashboard/_summary_cards.html.twig' %}
    {% include 'dashboard/_insights.html.twig' %}
    {% include 'dashboard/_priority_list.html.twig' %}
    {% include 'dashboard/_asset_breakdown.html.twig' %}
{% endblock %}
